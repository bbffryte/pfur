apiVersion: v1
kind: Service
metadata:
  name: sepft
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "sepft"
    port: 8080
  selector:
    app: sepft
